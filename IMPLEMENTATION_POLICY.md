# Blueberry Library 実装方針

## 役割分担

Blueberry Libraryは、AtCoder Library（ACL）と競合することを目的としない。

- ACLに収録されている標準的で広く使われる機能は、原則としてACLを利用する。
- Blueberry Libraryは、ACLにないデータ構造・アルゴリズムや、ACLの機能を競プロ向けに
  補う薄いラッパーを中心に収録する。
- ACLにある機能を学習目的で再実装する場合は、ACLと区別できる名前・パスにし、重複して
  利用者を迷わせない。

## ACLを優先する対象

以下のような、競技プログラミングで標準的な機能はACLを第一候補とする。

- `segtree`、`lazy_segtree`、`fenwick_tree`
- `dsu`、`scc_graph`、`two_sat`
- `mf_graph`、`mcf_graph`
- `modint`、`convolution`
- `suffix_array`、`lcp_array`、`z_algorithm`

ただし、ACLのAPIでは扱いにくい拡張や、異なる設計上の利点がある場合は、理由をドキュメントに
明記したうえで別実装を追加する。

### 形式的冪級数（FPS）の方針

FPSの係数型・NTT・一般的な多項式積はACLの `static_modint` と `convolution` を利用する。
ACLが提供しない級数演算（逆元、形式微分・積分、log、exp、sqrt、powなど）だけを
`blueberry::FormalPowerSeries` として実装する。入口は `blueberry/math/formal-power-series.hpp`、
短い別名は `blueberry::FPS<Mint>` とし、廃止した旧 `blueberry/fps.hpp` は復活させない。
NTT反復ではACL内部の `butterfly` を再利用して変換を使い回すが、これはACLの公開APIを
置き換えるものではなく、NTT対応modulus・変換長の前提をドキュメントで明示する。

## Blueberry Libraryで優先する対象

- ACLに収録されていないデータ構造（例: Wavelet Matrix、Li Chao Tree、永続データ構造）
- ACLの標準機能を組み合わせた、特定用途の便利なデータ構造
- Library Checkerなどの実問題で有用性が確認できる高度なアルゴリズム
- ACLでは提供されない設計（rollback、オンライン・オフラインの特殊クエリなど）

## 実装の採用手順

初回実装の時点で、性能調査・比較測定・既知の無駄の除去まで完了させる。
「まずACだけを取って公開し、性能は後で別作業にする」を標準手順にしない。
最速を普遍的に保証することはできないが、明らかな改善候補を未評価のまま残さない。
実装前に同等APIの候補と測定ケースを決め、実装中に構築・操作・I/O・メモリを測り、
同等契約で大幅な差がある場合は支配項の特定と妥当な改善候補の評価・修正を初回作業に含める。
汎用性を落として数値だけを近づけることはしない。棄却した候補と回帰もログへ残す。

1. Library Checkerの公式問題に対応する検証コードを用意する。
2. 公開されている複数の実装を調査し、計算量・実行時間・メモリ使用量の候補を比較する。
3. 候補を同じコンパイラ・オプションで再ベンチマークする。提出時の実行時間だけで採用を決めない。
4. 必要なアルゴリズムを、Blueberry LibraryのAPI方針に合わせて可読性のある形で実装する。
5. 公式テストケース、境界ケース、必要に応じて愚直解とのランダム比較で検証する。
6. 全公開操作の呼び出し方・計算量・使用例・注意点と、前提条件・制約・出典・ACLとの差分を記載する。

## APIとコードスタイル

- C++20、ヘッダオンリー、`blueberry`名前空間を基本とする。
- 添字は0-indexed、区間は半開区間 `[l, r)` を基本とする。
- マクロによる暗黙的な状態を避け、必要な型・演算・単位元をテンプレート引数で明示する。
- 一般的な機能はACLに近い命名にし、既存コードから移行しやすくする。
- 高速化は内部実装に閉じ込め、利用者側には短く安全なインターフェースを提供する。

## 出典とライセンス

公開提出や既存ライブラリは、まずアルゴリズムの調査資料として扱う。ライセンスが明確に
許可していないコードをそのままコピーせず、必要なら元のアルゴリズムをもとに再実装する。
コードや設計を直接利用した場合は、ファイル内コメントまたはドキュメントに出典とライセンスを記載する。

## 検証と公開

各ライブラリには対応する `verify/**/*.test.cpp` とドキュメントを用意する。`make verify` で
Library Checkerの公式テストケースを使って検証し、`main`へのマージ後にGitHub Actionsで再検証と
GitHub Pagesの更新を行う。

## ドキュメントの必須形式（今後の追加・変更にも適用）

- `templates/library.md.example` をもとに、概要・前提・メモリ計算量と、実行可能な最小使用例を書く。
- **すべての公開コンストラクタ・オーバーロード・関数・公開フィールド**を操作一覧に載せる。
- 一覧には呼び出し方（引数・返り値が分かる形）と計算量を併記する。
- 各操作に `<details class="api-operation" id="..." markdown="1">` を用意し、
  summaryに呼び出し方・計算量、内部に操作内容・使用例・注意点を記載する。
- 添字・空区間・空入力・更新順・数値の範囲・必要な代数的性質・参照の寿命などを明示する。
- O記法の変数を定義し、最悪・償却・前計算の違いを区別する。
- コードフェンスの外側をLiquidの `raw` / `endraw` タグで囲み、C++の連続した波括弧が
  テンプレート構文として処理されるのを防ぐ。テンプレートにはこの保護も含める。
- `.verify-helper/docs/static/_data/libraries.yml` に登録してカテゴリから辿れるようにする。
- `make test` で形式と最小使用例のコンパイル・実行を確認する。APIの説明漏れはレビューでも確認する。

## 実行時間の比較

`make verify` はACのタイムスタンプによる省略をせず、全verifyを毎回計測する。
コンパイル時間と解答実行時間を分け、標準では直列3回の実行合計の中央値を記録する。
公式checkerによる正誤判定はverification-helperの標準処理のまま利用する。
初回・失敗・計測欠損を0秒や改善として扱わない。

CPU・OS・ツール・コンパイラ・フラグ・テストデータ・verifyコード・反復数が同じ場合のみ
基準との差を表示する。共有runnerの負荷による差は残るため、微小な短縮で採用を判断しない。
PRの結果はmainの比較基準を上書きしない。ローカルでは `make benchmark-baseline` で
成功結果を基準として保存・置換し、その後の `make verify` で比較する。

### Library Checker Fastest の調査

`scripts/fetch_lc_fastest.py` は judge.yosupo.jp の公開 REST API を読み取り、AC 提出を
`+time` 順で取得する。上位提出のソースは `--save-source` を明示した場合だけ保存し、
NTT・SIMD・高速 I/O・ACL 畳み込みの有無などを機械的な手掛かりとしてレポートする。
Fastest の表示値は Yosupo 側の単一実行環境における最大ケース時間であり、CI の複数ケース集計とは
比較条件が異なる。採用判断は同一の入力・コンパイラ・フラグ・CPUで再ベンチマークした結果と、
可読性・移植性・ライセンスを合わせて行う。公開提出のコードをそのままコピーせず、アルゴリズムを
再実装する場合は出典をドキュメントに記載する。

### 汎用性を維持した高速化

Fastest調査は、既存APIの対応型・代数条件・問い合わせ順序・計算量・境界・tie・
失敗時動作を維持する改善に結び付ける。固定上限、可換/加算専用化、全クエリ先読み、
unsigned専用、特定ISA必須化による高速化を、汎用実装の置き換えとして採用しない。
最速提出にそれらが含まれる場合は、速度差を説明する条件として区別する。

優先するのは、portableな高速I/O、不要な計算・コピー・allocation・初期化・再圧縮・
階層伝播の削減、cacheに適した配置である。型の要件や所有権・再入性を強めない。
テンプレートの汎用性そのものを、測定せず遅さの原因と決めつけない。

性能比較では、同一入力について構造とI/Oを独立に替え、end-to-endに加えて
parse/format・構築・操作・メモリを分離する。releaseとassert有効の結果、
主要な演算・入力分布・N/Q比、GCC/Clangを区別する。前処理時間を省いた改善率や
異なるAPI保証の最速提出との比を、同条件の改善として報告しない。
同等契約で数倍の差が残る場合はACだけで完了扱いにせず、支配項と次の測定課題を残す。

具体的な測定と着手順は
[Fastestとの差を縮める方針](docs/development/fastest-performance-plan.md)を参照する。
